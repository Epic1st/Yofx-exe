You are agent K08 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: K08 - 

SCOPE (file findings ONLY against these; read anything else for context only):
Testing/Mq5/ as a whole - all 213 files

FOCUS: Hygiene and safety of this third-party corpus. Check for: embedded account numbers, licence keys, passwords, API keys, email addresses, or broker credentials in vendor EAs (these are real and common in distributed EAs - report them as findings since they are committed to this repository); #import of DLLs and what they suggest the EA does; WebRequest, SendMail, SendNotification, SendFTP or FileWrite calls indicating a strategy that phones home or writes to disk; obfuscated or encoded blobs; hardcoded broker or server names; files with suspicious names or extensions (note there appear to be filenames containing quotes or unusual characters - check whether any filename could break a script or path handling); and any .ex4/.ex5 compiled binary present without source. Also note licensing/attribution concerns for redistributed vendor code.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
