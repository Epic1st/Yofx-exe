You are agent L06 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: L06 - 

SCOPE (file findings ONLY against these; read anything else for context only):
RESEARCH LANE - search the web for PostgreSQL documentation on queue patterns and isolation, then compare against src/Apps/YO4X.ControlPlane.Workers/Operations/PostgresUserOperationWorkStore.cs and src/Apps/YO4X.ControlPlane.Workers/Outbox/ and src/BuildingBlocks/YO4X.Persistence.Postgres/

FOCUS: Research the canonical PostgreSQL guidance and compare. Cover: the SELECT FOR UPDATE SKIP LOCKED queue pattern and its correct use, including why a plain SELECT then UPDATE is unsafe; transaction isolation levels, what Read Committed does and does not protect against, and which anomalies require Repeatable Read or Serializable (and the serialisation-failure retry that Serializable then requires); advisory locks, session versus transaction scope, and the leak risk with connection pooling; row-level security semantics including the difference between ENABLE and FORCE and the fact that a table owner bypasses RLS unless forced; and how session state such as SET LOCAL interacts with a connection pool. For each, state the documented guidance, what this repository does, and the concrete failure. Cite sources; mark unconfirmed items PLAUSIBLE.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
