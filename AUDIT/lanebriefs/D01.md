You are agent D01 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: D01 - 

SCOPE (file findings ONLY against these; read anything else for context only):
src/BuildingBlocks/YO4X.Persistence.Postgres/Migrations/001_foundation.sql
002_user_operation_invocation_protocol.sql
003_pending_demo_broker_account_registration.sql

FOCUS: Schema correctness. For each table: is there a tenant column and is it NOT NULL; are foreign keys declared with the right ON DELETE behaviour (a missing FK allows orphans; CASCADE on a financial record silently destroys history); are uniqueness constraints present where the code assumes uniqueness (especially idempotency keys); nullability matching what the code expects; CHECK constraints on enum-like text columns and on non-negative amounts; numeric column types for money (numeric with explicit precision, not double precision or float8); timestamp columns using timestamptz rather than timestamp (a naive timestamp loses the offset and shifts every trade time); and indexes for the predicates the code actually queries on, including the tenant column.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
