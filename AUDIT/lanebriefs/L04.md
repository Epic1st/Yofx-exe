You are agent L04 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: L04 - 

SCOPE (file findings ONLY against these; read anything else for context only):
RESEARCH LANE - search the web for advisories, then compare against Directory.Packages.props, all *.csproj, and global.json

FOCUS: First read Directory.Packages.props, global.json and the csproj files to establish the exact package and SDK versions in use. Then research known vulnerabilities and documented misuse patterns for those specific versions - Npgsql, the Roslyn/CodeAnalysis packages, the ASP.NET Core and Microsoft.Extensions packages, any serialization library, and the .NET SDK/runtime version pinned in global.json. Report: any package version with a known advisory (name the advisory and the fixed version), and any documented dangerous-by-default configuration for these libraries that this repository actually uses. Be rigorous about version applicability - a CVE that does not apply to the pinned version is noise. Cite sources; mark anything you could not confirm as PLAUSIBLE rather than asserting it.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
