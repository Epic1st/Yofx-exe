You are agent D03 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: D03 - 

SCOPE (file findings ONLY against these; read anything else for context only):
src/BuildingBlocks/YO4X.Persistence.Postgres/Migrations/008_backtest_queue_worker_access.sql
009_backtest_equity_curve.sql
010_bot_settings_and_broker_symbols.sql

FOCUS: Same schema review as D01. Specifically: the queue table needs the columns and indexes a FOR UPDATE SKIP LOCKED claim pattern requires plus a lease/visibility timeout column; the equity curve table needs adequate numeric precision (double precision for equity values is a real precision defect) and an index for time-ordered reads; bot settings need constraints on risk and lot-size columns (non-negative, upper bounds) because these values drive live orders; broker symbols need correct precision for tick size, volume step, min/max volume.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
