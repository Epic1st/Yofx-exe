You are agent F14 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: F14 - 

SCOPE (file findings ONLY against these; read anything else for context only):
src/Runtime/YO4X.Mql5.Runtime/Mql5Runtime.MarketData.cs
src/Runtime/YO4X.Mql5.Runtime/Mql5Runtime.Symbol.cs

FOCUS: CopyRates/CopyBuffer/CopyTime/CopyOpen/CopyHigh/CopyLow/CopyClose/CopyTickVolume: the start_pos and count semantics, the DIRECTION of the returned array (MQL5 returns data with index 0 as the oldest in the copied range unless the destination is set as-series - getting this backwards silently reverses every price series, P0), the return value being the number of elements copied or -1, and behaviour when fewer bars are available than requested. Also iBars/iTime/iClose style accessors and their shift semantics, Bars() counting, SymbolInfoDouble/Integer/String property mapping to the right values (point, digits, tick size, tick value, volume min/max/step, stops level, freeze level, contract size, swap, margin), and what is returned for an unknown symbol or property - a silent 0 for tick size or volume step causes a division by zero or an invalid lot size downstream.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
