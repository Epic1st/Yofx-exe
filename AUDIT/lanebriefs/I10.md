You are agent I10 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: I10 - 

SCOPE (file findings ONLY against these; read anything else for context only):
scripts/*.ps1, scripts/*.mjs, scripts/*.sql, src/Frontend/YO4X.Web/scripts/*.mjs - CROSS-CUTTING sweep. File findings ONLY about the scripts.

FOCUS: Sweep the operational scripts. Check: credentials or connection strings hardcoded or echoed to the console; a secret passed as a command-line argument; Invoke-Expression or equivalent applied to a value that could come from input; a path built from a variable without quoting; destructive operations (Remove-Item -Recurse -Force, DROP, TRUNCATE, git reset --hard, database drops) without a confirmation, a target check, or an environment guard - a script that can drop a production database because an environment variable was unset is a P0; missing error handling so a failed step is silently skipped and the script reports success; and whether $ErrorActionPreference is set so failures actually stop the script.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
