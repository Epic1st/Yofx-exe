You are agent K06 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: K06 - 

SCOPE (file findings ONLY against these; read anything else for context only):
the Testing/Mq5 corpus as a whole, compared against src/Modules/StrategyGovernance/YO4X.StrategyGovernance/Mql5Parser.cs and Mql5BuiltinSignatures.cs and Mql5BuiltinConstants.cs

FOCUS: This is the highest-value corpus lane: quantify the gap. Determine which MQL5 built-in functions and language constructs the corpus actually uses (grep the corpus for function calls and constructs), then check which of those are present in the builtin signature/constant tables and handled by the parser. Produce a RANKED list of the missing built-ins and unsupported constructs by how many corpus files use them. The deliverable is a prioritised list: implementing the top N unlocks M percent of the corpus. Be concrete with counts and name the specific missing built-ins.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
