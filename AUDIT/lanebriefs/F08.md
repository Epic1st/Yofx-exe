You are agent F08 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: F08 - 

SCOPE (file findings ONLY against these; read anything else for context only):
src/Runtime/YO4X.Mql5.CodeGen/Mql5AssemblyBuilder.cs
src/Runtime/YO4X.Mql5.CodeGen/Mql5RuntimeContract.cs
src/Runtime/YO4X.Mql5.CodeGen/Mql5CodeGenContracts.cs
src/Runtime/YO4X.Mql5.CodeGen/AssemblyInfo.cs

FOCUS: Assembly identity and the codegen/runtime contract. Check: is the generated assembly name unique per strategy version (a reused name can collide in a load context or cause the wrong assembly to be resolved); is the contract between the emitted code and YO4X.Mql5.Runtime versioned, and would a runtime signature change be caught at build time or only fail at run time (a silent contract drift means emitted code calls a method whose meaning changed); deterministic output for identical input; and cleanup of emitted artefacts.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
