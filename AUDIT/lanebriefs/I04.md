You are agent I04 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: I04 - 

SCOPE (file findings ONLY against these; read anything else for context only):
whole tree - CROSS-CUTTING sweep. File findings ONLY about exception handling.

FOCUS: Sweep for swallowed and mishandled exceptions. Look for: empty catch blocks; catch blocks that only log and then continue as if the operation succeeded; catch (Exception) around a money path (order placement, balance update, position close) where the caller cannot distinguish failure from success; a caught exception that returns a default/success-shaped value (returning false, null, or an empty collection where the caller treats it as a legitimate empty result); catch blocks that lose the original exception (throw ex resetting the stack trace); exceptions swallowed inside a background loop so the loop dies or silently stops making progress; and finally/using blocks that can themselves throw and mask the original error. Prioritise the trading, worker, and persistence paths - a swallowed exception there means an order or a state change silently did not happen. Report the worst instances with file:line and state what silently fails.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
