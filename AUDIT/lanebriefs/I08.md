You are agent I08 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: I08 - 

SCOPE (file findings ONLY against these; read anything else for context only):
all appsettings*.json, Properties/launchSettings.json, compose.yaml, .env.example, global.json, Directory.Build.props, *.pubxml - CROSS-CUTTING sweep. File findings ONLY about configuration.

FOCUS: Sweep configuration for insecure defaults and environment bleed. Check: any credential, key, or connection string with a real-looking value committed; development settings that would apply in production (detailed errors, Swagger, permissive CORS, disabled HTTPS redirection, a development authentication scheme); a required setting with an insecure fallback when absent; logging levels that would emit sensitive data; compose.yaml exposing a database or internal service port publicly, using default credentials, or running without TLS; a database connection with SSL Mode=Disable or Prefer; and settings that differ between hosts in a way that suggests one was forgotten. For each, say which environment is affected and what the consequence is.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
