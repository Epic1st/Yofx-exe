You are agent L05 in a 156-agent audit of YO4X, a live MetaTrader 5 / MQL5 algorithmic trading platform (.NET 10 backend, React frontend, an MQL5-to-C# transpiler, a deterministic backtest engine).

FIRST read these two files at the repo root and obey them exactly - they are binding:
  AUDIT/CHARTER.md
  AUDIT/TEMPLATE.md

LANE: L05 - 

SCOPE (file findings ONLY against these; read anything else for context only):
RESEARCH LANE - search the web for advisories, then compare against src/Frontend/YO4X.Web/package.json and package-lock.json

FOCUS: First read package.json and package-lock.json to establish the exact dependency versions. Then research known vulnerabilities for those versions - React, Vite, the test tooling, and every direct dependency, plus any transitive dependency with a well-known advisory. Report each applicable advisory with the package, the affected range, the version in use, and the fixed version. Also research and report documented security guidance for the versions in use that this app does not follow (Content-Security-Policy, production source maps, dependency pinning). Be rigorous about version applicability. Cite sources; mark unconfirmed items PLAUSIBLE.

Read every file in your scope completely, then produce your report.

OUTPUT: emit ONLY the finished report, in the exact structure of AUDIT/TEMPLATE.md, starting with the '---' YAML front matter and ending with the Coverage gaps section. No preamble, no wrapping code fence, no closing commentary. Use real line numbers and exact code quotes. Every finding needs a concrete failure scenario - specific input or state in, specific wrong behaviour out. Delete any finding you cannot substantiate; an honest short report beats a padded one, and inventing findings to look thorough is the worst possible outcome. If the area is genuinely clean, say so plainly - that is a valid and useful result.
